<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/header"
        layout="@layout/toolbar" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerCategory"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/header"
        android:layout_marginLeft="@dimen/_6sdp"
        android:layout_marginRight="@dimen/_6sdp"></android.support.v7.widget.RecyclerView>

    <tr.xip.errorview.ErrorView
        android:id="@+id/errorView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="@dimen/_60sdp"
        android:layout_marginTop="@dimen/_60sdp"
        android:visibility="gone"
        app:ev_retryButtonBackground="@color/colorPrimaryDark"
        app:ev_retryButtonTextColor="@color/colorWhite"
        app:ev_subtitleColor="@android:color/darker_gray"
        app:ev_titleColor="@android:color/darker_gray" />

</RelativeLayout>